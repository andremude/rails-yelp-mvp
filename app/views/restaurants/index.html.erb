<h1>Restaurants: </h1>
<div class="container">
<ol>
<% @restaurants.each do |r| %>
  <li class="restaurant"><%=link_to r.name, restaurant_path(r) %></li>
<% end %>
</ol>
  <%= link_to "Add Restaurant", new_restaurant_path(@restaurant) %>
</div>
